<%= turbo_frame_tag "admin_dash" do %>

    <table class="table">
        <thead>
            <tr>
                <th scope="col">Email</th>
                <th scope="col">Cash</th>
                <th scope="col">Status</th>
                <th scope="col">Confirmed?</th>
            </tr>
        </thead>
        <tbody>
        <% @users.each do |user| %>
            <tr>
                <td><%= user.email %></td>
                <td><%= user.cash %></td>
                <td class="text-center"><%= user.approved ? "1" : "0" %></td>
                <td class="text-center"><%= user.confirmed? %></td>
            </tr>
        <% end %>
        </tbody>
    </table>

<% end %>